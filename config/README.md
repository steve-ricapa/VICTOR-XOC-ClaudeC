# Local Config

Local per-client configuration lives here and is intentionally not versioned.

Expected files:
- `agent.yaml`
- `policy.yaml`
- `capabilities.yaml`
- `mcp.yaml`

Use `config.dist/` as the source of versioned templates.
