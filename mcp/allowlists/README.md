# MCP Allowlists

Keep MCP allowlist policy files in this directory.

Recommended content:
- client id / tenant id
- explicitly allowed tools
- optional deny list
- optional notes and change history

Example file:
- `default_allowlist.example.yaml`
