# MCP Adapters

Place MCP adapter configuration and mapping assets here.

Typical fields:
- `tool_name`
- `adapter_class` or runtime binding key
- request/response mapping rules
- retry and timeout settings

Example file:
- `openvas_adapter.example.yaml`
