# MCP Manifests

Store MCP tool/service manifests used by on-prem deployments.

Typical fields:
- `tool_name`
- `description`
- `endpoint`
- `auth` requirements
- `timeouts`
- `capability` mapping hint

Example file:
- `openvas.example.yaml`
